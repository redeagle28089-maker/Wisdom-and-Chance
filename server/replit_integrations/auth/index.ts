export { setupAuth, isAuthenticated, getSession } from "./replitAuth";
export { authStorage, ProviderConflictError, type IAuthStorage } from "./storage";
export { registerAuthRoutes } from "./routes";
